/**
 * Local Vite entry when running `vite` / `npx vite` from apps/cloud-license-server.
 * Without this file Vite walks up to the monorepo root and starts the main EDMS app instead.
 */
export { default } from "./web/vite.config";
