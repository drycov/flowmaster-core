# ЕСЭДО — полный Docker-стек: self-hosted Supabase + app + cron
name: flowmaster

include:
  - path:
      - ./docker/supabase/docker-compose.yml
      - ./docker/compose/supabase.overrides.yml
      - ./docker/compose/flowmaster.services.yml
